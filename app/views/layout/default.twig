<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Job Dating Platform{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    {% if session().has('flash') %}
        <div class="fixed top-4 right-4 p-4 rounded {% if session().get('flash').type == 'success' %}bg-green-500{% else %}bg-red-500{% endif %} text-white">
            {{ session().get('flash').message }}
        </div>
    {% endif %}

    <div class="container mx-auto px-4 py-8">
        {% block content %}{% endblock %}
    </div>
</body>
</html>